{
  "version": 2,
  "builds": [
    {
      "src": "src/front/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "src/front/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
